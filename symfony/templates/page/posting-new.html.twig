{% extends 'base.html.twig' %}

{% block title %}
Новый пост
{% endblock %}

{% block content %}
<h1>Новый пост</h1>
<p>Введите новый пост</p>
    {{ form_start(postingForm) }}
    {{ form_row(postingForm.subject, { label: 'Введите тему', errors: false, attr: { placeholder: 'Тема' }}) }}
    {{ form_row(postingForm.message, { label: 'Сообщение', name: 'message', errors: false, attr: { placeholder: 'Сообщение' }}) }}

    <button type="submit" class="btn btn-success">Ввод</button>
    {{ form_end(postingForm) }}

    {{ errorsForm }}
{#
<form action="{{ path('posting-form') }}" method="post" >
    <div class="form-group">
        <label for="subject">Введите тему</label>
        <input type="text" name="subject" placeholder="Тема" id="subject" class="form-control">
    </div>
    <div class="form-group">
        <label for="message">Сообщение</label>
        <textarea type="text" name="message" placeholder="Сообщение" id="message" class="form-control"></textarea>
    </div>
    <input type="hidden" name="token" value="{{ csrf_token('posting-form') }}">
    <button type="submit" class="btn btn-success">Ввод</button>
</form>
#}
{% endblock %}
